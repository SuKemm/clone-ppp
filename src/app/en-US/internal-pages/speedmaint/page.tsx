import { InternalPageTemplate } from "@/components/InternalPageTemplate";

// TODO: set the real Speedmaint system URL once available.
export default function SpeedmaintPageEn() {
  return (
    <InternalPageTemplate
      title="Speedmaint"
      description="Equipment maintenance management system."
      externalUrl=""
      externalLabel="Open Speedmaint"
    />
  );
}
