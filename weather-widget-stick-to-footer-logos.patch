diff --git a/src/app/en-US/page.tsx b/src/app/en-US/page.tsx
index 77a9b84..6578d52 100644
--- a/src/app/en-US/page.tsx
+++ b/src/app/en-US/page.tsx
@@ -164,11 +164,14 @@ export default function EnglishHomePage() {
       {/* ===== Running text (replaces the old investor logo block) ===== */}
       <MarqueeBar isEnglish={true} />
 
-      {/* Bọc "News" + "Production" trong 1 khối "relative" cao vừa đủ để
-          widget thời tiết bên trong có chỗ "sticky" (dính) khi cuộn — ban
-          đầu nó nằm ngay dưới dòng chữ chạy như nội dung bình thường, cuộn
-          tới đâu widget mới bắt đầu dính theo dưới header tới đó, hết khối
-          này thì nhả ra cuộn tiếp cùng trang. */}
+      {/* Wraps the rest of the homepage (News, Production, Investor
+          Relations, Library...) in one "relative" block so the weather
+          widget inside can go "sticky" while scrolling — it starts out
+          right below the running text like normal content, then sticks
+          under the header as you scroll, staying stuck all the way to the
+          end of the page before letting go (right as the page reaches the
+          investor-logo marquee in the footer — see ptsc-shell.tsx, kept
+          outside this block since it's shared across every page). */}
       <div className="relative">
         <div className="pointer-events-none absolute inset-0 z-20">
           <div className="sticky top-[88px] w-fit pl-3 pt-4 sm:top-[104px] sm:pl-4 lg:top-[124px]">
@@ -343,7 +346,12 @@ export default function EnglishHomePage() {
           </div>
         </div>
         </section>
-      </div>
+
+      {/* The "relative" wrapper around the sticky weather widget now extends
+          down through the sections below (Investor Relations, Library...) —
+          the widget stays "stuck" for the whole homepage body, only letting
+          go once {children} ends inside PtscShell, right as the page reaches
+          the investor-logo marquee in the footer (see ptsc-shell.tsx). */}
 
       <section id="quan-he-co-dong" className="mx-auto max-w-7xl px-6 py-16 lg:px-8">
         <div className="overflow-hidden rounded-[1.5rem] border border-slate-200 shadow-sm">
@@ -451,6 +459,7 @@ export default function EnglishHomePage() {
           </div>
         </div>
       </section>
+      </div>
     </PtscShell>
   );
 }
diff --git a/src/app/page.tsx b/src/app/page.tsx
index 0fa2685..7c028c0 100644
--- a/src/app/page.tsx
+++ b/src/app/page.tsx
@@ -162,11 +162,13 @@ export default function Home() {
       {/* ===== Dòng chữ chạy (thay cho khối logo cổ đông cũ) ===== */}
       <MarqueeBar isEnglish={false} />
 
-      {/* Bọc "News" + "Sản lượng" trong 1 khối "relative" cao vừa đủ để
-          widget thời tiết bên trong có chỗ "sticky" (dính) khi cuộn — ban
-          đầu nó nằm ngay dưới dòng chữ chạy như nội dung bình thường, cuộn
-          tới đâu widget mới bắt đầu dính theo dưới header tới đó, hết khối
-          này thì nhả ra cuộn tiếp cùng trang. */}
+      {/* Bọc toàn bộ nội dung còn lại của trang chủ (News, Sản lượng, Quan hệ
+          cổ đông, Thư viện...) trong 1 khối "relative" để widget thời tiết
+          bên trong có chỗ "sticky" (dính) khi cuộn — ban đầu nó nằm ngay dưới
+          dòng chữ chạy như nội dung bình thường, cuộn tới đâu widget dính
+          theo dưới header tới đó, dính suốt cho tới hết trang thì mới nhả ra
+          (đúng lúc chạm dải logo cổ đông chạy ở chân trang — xem
+          ptsc-shell.tsx, nằm ngoài khối này vì dùng chung cho mọi trang). */}
       <div className="relative">
         <div className="pointer-events-none absolute inset-0 z-20">
           <div className="sticky top-[88px] w-fit pl-3 pt-4 sm:top-[104px] sm:pl-4 lg:top-[124px]">
@@ -342,7 +344,12 @@ export default function Home() {
           </div>
         </div>
       </section>
-      </div>
+
+      {/* Khối "relative" bọc sticky widget thời tiết được kéo dài xuống hết
+          các section bên dưới (Quan hệ cổ đông, Thư viện...) — tức là widget
+          giờ "dính" theo trong suốt phần nội dung trang chủ, chỉ nhả ra khi
+          hết {children} bên trong PtscShell, đúng lúc chạm tới dải logo cổ
+          đông chạy ở chân trang (nằm ngoài page.tsx, xem ptsc-shell.tsx). */}
 
       <section id="quan-he-co-dong" className="mx-auto max-w-7xl px-6 py-16 lg:px-8">
         <div className="overflow-hidden rounded-[1.5rem] border border-slate-200 shadow-sm">
@@ -452,6 +459,7 @@ export default function Home() {
           </div>
         </div>
       </section>
+      </div>
     </PtscShell>
   );
 }
